# Tender RAG POC — Python 3.12
# MCP server: stdio for local AI clients, streamable HTTP for hosting. In --http
# mode the same process also serves the browser chat page and the REST API.
mcp==2.*
# REST layer mounted at /api (routers + Swagger at /api/docs). Must stay
# unpinned-ish: 0.115 required starlette<0.47, which mcp 2.x contradicts.
fastapi>=0.141
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pgvector==0.3.*
pydantic==2.*
pydantic-settings==2.*
python-dotenv==1.*
httpx==0.27.*
tiktoken==0.*
fastembed==0.8.*        # in-process ONNX embeddings (bge-small, 384-dim)
# chat via LangChain over any OpenAI-compatible endpoint (Meta Llama API = Llama 4,
# Groq gpt-oss backup). langchain-openai pulls langchain-core + the openai client.
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.4
