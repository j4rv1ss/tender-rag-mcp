# Tender RAG POC — Python 3.12
# MCP server (stdio transport). Replaces the old FastAPI/uvicorn HTTP layer:
# the corpus is exposed as MCP tools to clients like Claude Desktop / Claude Code.
mcp==2.*
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pgvector==0.3.*
pydantic==2.*
pydantic-settings==2.*
python-dotenv==1.*
httpx==0.27.*
tiktoken==0.*
fastembed==0.8.*        # in-process ONNX embeddings (bge-small, 384-dim)
# chat via LangChain over any OpenAI-compatible endpoint (Meta Llama API = Llama 4,
# Groq gpt-oss backup). langchain-openai pulls langchain-core + the openai client.
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.4
